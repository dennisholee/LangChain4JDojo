# Test overrides — these values are used by AppConfig in unit tests only.
# No real API key is needed for the unit tests (adapters are stubbed).

openai.embedding.model=text-embedding-3-small
openai.chat.model=gpt-4o-mini
vectorstore.chroma.base-url=http://localhost:8000
vectorstore.chroma.collection=test-composable-retriever
retriever.max-results=3
retriever.min-score=0.0
demo.query=What is composable retrieval and RAG?
